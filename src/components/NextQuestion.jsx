import { QuizContext } from "../contexts/Quiz"

const NextQuestion = ({answers}) => {

    if (indexOf(answers) = answers.lenght()) {
        setStep(step ++)
    }

    return (
        <QuizContext>
            
        </QuizContext>
    )
}

export default NextQuestion